const Logout = () => {
  return (
    <img
      src={"/assets/icons/logout.svg"}
      alt={"back"}
      loading="lazy"
      style={{
        width: "25px",
        height: "25px",
      }}
    />
  );
};

export default Logout;
